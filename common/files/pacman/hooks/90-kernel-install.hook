[Trigger]
Type = Path
Operation = Install
Operation = Upgrade
Target = usr/lib/modules/*/vmlinuz

[Action]
Description = Generating Unified Kernel Image...
When = PostTransaction
Exec = /bin/sh -c 'while read -r f; do kernel-install add "$(basename "$(dirname "$f")")" "/$f"; done'
NeedsTargets
